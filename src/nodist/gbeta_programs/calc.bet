-- betaenv:descriptor --
(#
 calcUnit: (##);
 adder: calcUnit(##);
 subtracter: calcUnit(##);
 divider: calcUnit(##);
 multiplier: calcUnit(##);
 userinputinterpreter: (##);
do
(* main loop *)
 
  (* get input *)
   
  (* calculate result *)

  (* display output *)
#)

